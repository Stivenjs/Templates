version: 1
applications:
  - frontend:
      buildPath: '/'  # Ejecutar desde la raíz del monorepo
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build --workspace=Anvogue
      artifacts:
        baseDirectory: apps/Anvogue/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: apps/Anvogue

  - frontend:
      buildPath: '/'  # Ejecutar desde la raíz del monorepo
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build --workspace=Ninico
      artifacts:
        baseDirectory: apps/Ninico/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: apps/Ninico
